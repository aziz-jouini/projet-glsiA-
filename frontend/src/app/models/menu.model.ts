export interface Menu {
    id?: number;
    name: string;
    description: string;
    imageId?: string; // Remarquez le changement ici pour correspondre à la propriété utilisée dans l'API REST
  }
  